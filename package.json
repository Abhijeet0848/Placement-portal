{
  "name": "smart-placement-portal-root",
  "version": "1.0.0",
  "description": "Smart Placement Portal - AI Powered Campus Recruitment Platform",
  "scripts": {
    "install-all": "npm install && npm install --prefix client && npm install --prefix server",
    "dev": "npx concurrently \"npm run dev --prefix client\" \"npm run dev --prefix server\"",
    "build-all": "npm run build --prefix client && npm run build --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
